{
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "name": "memory-real-time",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:fe": "yarn workspace frontend start",
    "dev:be": "yarn workspace backend start:dev",
    "typeorm": "ts-node -r tsconfig-paths/register node_modules/typeorm/cli.js --config /src/ormconfig.ts",
    "migrate:create": "yarn workspace backend typeorm:migrate",
    "migrate:run": "yarn workspace backend typeorm:run",
    "lint:be": "yarn workspace backend lint"
  },
  "engines": {
    "node": "12.16.1"
  }
}
